import { jsx as _jsx } from "react/jsx-runtime";
import QueryButton from "./QueryButton";
import useAllQueries from "../../_hooks/useAllQueries";
export default function QueryList({ selectedQuery, setSelectedQuery }) {
    // Holds all query keys
    const queryKeys = useAllQueries();
    return (_jsx("div", { className: "flex flex-col w-full ", children: queryKeys.map((query, inex) => {
            return (_jsx(QueryButton, { selected: selectedQuery, setSelected: setSelectedQuery, query: query }, inex));
        }) }));
}
//# sourceMappingURL=QueryList.js.map