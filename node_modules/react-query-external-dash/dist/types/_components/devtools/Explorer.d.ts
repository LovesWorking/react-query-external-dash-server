import { Query, QueryKey } from "@tanstack/react-query";
import { Socket } from "socket.io-client";
import { User } from "../../_types/User";
type Props = {
    currentUser: User;
    socket: Socket;
    editable?: boolean;
    label: string;
    value: any;
    defaultExpanded?: Array<string>;
    activeQuery?: Query<unknown, Error, unknown, QueryKey> | undefined;
    dataPath?: Array<string>;
    itemsDeletable?: boolean;
};
export default function Explorer({ currentUser, socket, editable, label, value, defaultExpanded, activeQuery, dataPath, itemsDeletable, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
