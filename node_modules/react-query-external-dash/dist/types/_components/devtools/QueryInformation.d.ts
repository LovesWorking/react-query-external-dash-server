import { Query } from "@tanstack/react-query";
import { Socket } from "socket.io-client";
import { User } from "../../_types/User";
interface Props {
    selectedQuery: Query | undefined;
    socket: Socket;
    currentUser: User;
}
export default function QueryInformation({ selectedQuery, socket, currentUser, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
