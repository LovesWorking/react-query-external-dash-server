import { Query } from "@tanstack/react-query";
import { Socket } from "socket.io-client";
import { User } from "../../_types/User";
interface Props {
    allQueries: Query[];
    socket: Socket;
    currentUser: User;
}
export default function QueriesList({ allQueries, socket, currentUser, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
