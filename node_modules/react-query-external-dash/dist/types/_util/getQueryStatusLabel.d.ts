import { ExtendedQuery } from "../_types/QueryExternal";
type QueryStatus = "fetching" | "inactive" | "paused" | "stale" | "fresh";
export declare function getQueryStatusLabel(query: ExtendedQuery): QueryStatus;
export {};
